def count_diff_than(tweets):
	"""
	Given the list of tweets, from the user_timeline API endpoint 
	Count the number of statuses that contain the phrase different than

	Arguments:
		a list of twitter tweet objects that are dicts
	Returns:
		A count of the number of statuses using "different than"
	"""

def custom_message(count):
	"""
	Create a custom reply to a user who uses different than
	Arguments:
		Count the number of tweets containing different than
	Returns: 
		A text string containing a message, the number of 'different than' tweets
		and a url to a page explaining the difference
	"""

	
